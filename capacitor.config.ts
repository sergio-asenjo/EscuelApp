import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duoc.escuelapp',
  appName: 'EscuelApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
